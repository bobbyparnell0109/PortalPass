# PortalPass ships pointing at the shared demo Supabase project. To use
# your own project instead, copy this file to .env and fill these in, then
# run supabase/migrations + supabase/seed.sql against it.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
